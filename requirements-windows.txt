-r requirements.txt
# pywin32 is not actually imported directly (collector_windows.py uses
# plain ctypes for GetForegroundWindow/GetLastInputInfo, which needs no
# extra package), but PyInstaller's own Windows hooks work more reliably
# with it present -- see packaging/build_windows.md.
pywin32>=306; sys_platform == "win32"
